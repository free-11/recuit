<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.recruit.mapper.StudentMapper">
    
    <!-- 查询所有学生 -->
    <select id="selectAll" resultType="org.example.recruit.entity.Student">
        SELECT id, name, student_num, grade, specialty, qq, phone, submission_time
        FROM student
    </select>
    
    <!-- 根据ID查询学生 -->
    <select id="selectById" parameterType="java.lang.Long" resultType="org.example.recruit.entity.Student">
        SELECT id, name, student_num, grade, specialty, qq, phone, submission_time
        FROM student
        WHERE id = #{id}
    </select>
    
    <!-- 插入学生 -->
    <insert id="insert" parameterType="org.example.recruit.entity.Student">
        INSERT INTO student (name, student_num, grade, specialty, qq, phone, submission_time)
        VALUES (#{name}, #{studentNum}, #{grade}, #{specialty}, #{qq}, #{phone}, #{submissionTime})
    </insert>
    
    <!-- 更新学生 -->
    <update id="update" parameterType="org.example.recruit.entity.Student">
        UPDATE student
        SET name = #{name}, student_num = #{studentNum}, grade = #{grade}, 
            specialty = #{specialty}, qq = #{qq}, phone = #{phone}, submission_time = #{submissionTime}
        WHERE id = #{id}
    </update>
    
    <!-- 删除学生 -->
    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM student
        WHERE id = #{id}
    </delete>
</mapper>